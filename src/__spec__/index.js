describe('Promise A+ spec:', () => {
  descript('Promise states', () => {

  })

  describe('"Then" method', () => {
    describe('When pending a promise:', () => {
      test('may transition to either the fulfilled or rejected state.',  () => {

      })
    })
  })
})
